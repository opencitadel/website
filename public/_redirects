# Cloudflare Pages redirects.
#
# The vanity path exists so the invite can be rotated without editing every
# link across three repositories.

/discord  https://discord.gg/KdJdfabkDM  302

# Convenience paths for the published artifacts.
/model.json        /oars-model.json        302
/permissions.json  /oars-permissions.json  302

# Per-standard subdomains are handled by a Cloudflare redirect rule rather than
# here, because _redirects cannot match on hostname. See DEPLOYMENT.md.
