{
  "name": "@opencitadel/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "open-citadel.org — one site for every Open Citadel standard.",
  "scripts": {
    "dev": "node scripts/sync-content.mjs && astro dev",
    "build": "node scripts/sync-content.mjs && astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-content.mjs",
    "check-links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.1.3",
    "sharp": "^0.35.3",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
